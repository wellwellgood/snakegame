[build]
base = "ait-app"
command = "npm ci --omit=dev --no-audit --no-fund && npm run web:build"
publish = "ait-app/build"

[build.environment]
NODE_VERSION = "18.20.3"   # 또는 프로젝트가 요구하는 정확 버전
NPM_FLAGS = "--omit=dev"